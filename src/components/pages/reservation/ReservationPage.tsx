import Layout from '@components/ui/Layout'
import { useHeader } from '@hooks/useHeader'
import React from 'react'

const ReservationPage: React.FC = () => {
  useHeader(true, 'Make a reservation')
  return (
    <Layout>
      
    </Layout>
  )
}

export default ReservationPage
